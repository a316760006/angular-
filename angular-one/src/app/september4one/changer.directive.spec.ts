import { ChangerDirective } from './changer.directive';

describe('ChangerDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangerDirective();
    expect(directive).toBeTruthy();
  });
});
