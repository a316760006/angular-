import { ChangeronePipe } from './changerone.pipe';

describe('ChangeronePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeronePipe();
    expect(pipe).toBeTruthy();
  });
});
